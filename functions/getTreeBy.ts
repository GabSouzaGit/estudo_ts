type Tree = {
    node : number | undefined,
    left? : Tree,
    right? : Tree
}

function getTreeBy(array : number){
    const tree : Tree = { node: undefined };

    const generate = (i : number, skip : number) => {
        if(){
            
        }
    }
}